{
    "id": "a1a80c2c-7649-49f7-8417-8169a5a295c4",
    "parent_ids": [
        "d9f35a33-8412-49f3-a112-c357462dea8b"
    ],
    "fitness": "-inf",
    "feedback": "Optimizer failed during initial dry run: Subclasses must implement __call__",
    "code": "import numpy as np\n\nclass SimulatedAnnealingOptimizer:\n    def __init__(self, budget: int, dim: int, initial_temp=10.0, cooling_rate=0.95):\n        self.budget = budget\n        self.dim = dim\n        self.evals = 0\n        self.best_f = float('inf')\n        self.best_x = np.zeros(dim)\n        self.materials = list(range(6))  # Assuming categorical variables are IDs from 0 to 5\n        self.temperature = initial_temp\n        self.cooling_rate = cooling_rate\n\n    def _evaluate(self, x, func):\n        \"\"\"Wrapper to safely track budget and update best solution.\"\"\"\n        if self.evals >= self.budget:\n            return float('inf')\n        f = func(x)\n        self.evals += 1\n        if f < self.best_f:\n            self.best_f = f\n            self.best_x = x.copy()\n        return f\n\n    def _acceptance_probability(self, old_f, new_f):\n        \"\"\"Compute acceptance probability based on the Metropolis criterion.\"\"\"\n        if new_f < old_f:\n            return 1.0\n        else:\n            return np.exp((old_f - new_f) / self.temperature)\n\n    def __call__(self, func):\n        # Initialization (LHS sampling)\n        initial_population = lhs(n_samples=20, n_dim=self.dim)\n        for x in initial_population:\n            self._evaluate(x, func)\n\n        current_x = self.best_x.copy()\n        current_f = self.best_f\n\n        while self.evals < self.budget:\n            # Generate a new candidate solution\n            candidate_x = current_x.copy()\n\n            # Gaussian mutation for continuous variables\n            candidate_x[:18] += np.random.normal(0, 0.1, 18)\n            candidate_x[:18] = np.clip(candidate_x[:18], -1, 1)  # Ensure bounds\n\n            # Random perturbation for categorical variables\n            for i in range(18, 24):\n                if np.random.rand() < 0.1:\n                    candidate_x[i] = np.random.choice(self.materials)\n\n            candidate_f = func(candidate_x)\n            \n            # Acceptance decision\n            ap = self._acceptance_probability(current_f, candidate_f)\n            if np.random.rand() < ap:\n                current_x = candidate_x.copy()\n                current_f = candidate_f\n\n            # Update best solution\n            self._evaluate(current_x, func)\n\n            # Cool down the temperature\n            self.temperature *= self.cooling_rate\n\n        return self.best_f, self.best_x",
    "generation": 29,
    "name": "SimulatedAnnealingOptimizer"
}