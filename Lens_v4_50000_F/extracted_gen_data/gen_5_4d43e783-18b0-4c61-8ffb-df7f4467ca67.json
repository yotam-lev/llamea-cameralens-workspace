{
    "id": "4d43e783-18b0-4c61-8ffb-df7f4467ca67",
    "parent_ids": [
        "dc992505-fc44-448a-94f1-e217d883b42c"
    ],
    "fitness": -0.28922811794690667,
    "feedback": "Mean loss: 0.289228. Best single run: 0.289228.",
    "code": "import numpy as np\nfrom scipy.optimize import minimize\n\ntry:\n    from pyDOE2 import lhs as _lhs\nexcept ImportError:\n    try:\n        from scipy.stats import qmc\n        def _lhs(n_samples, n_dim):\n            return qmc.LatinHypercube(d=n_dim).random(n=n_samples)\n    except ImportError:\n        raise ImportError(\"pyDOE2 or scipy required\")\n\nclass Optimizer:\n    def __init__(self, budget: int, dim: int):\n        self.budget = budget\n        self.dim = dim\n        self.evals = 0\n        self.best_f = float('inf')\n        self.best_x = np.random.uniform(-1, 1, dim)\n        self.pop_size = max(4, int(budget / 35))\n        self.rng = np.random.default_rng(42)\n        \n    def _evaluate(self, x, func):\n        if self.evals >= self.budget: \n            return float('inf')\n        x_clean = np.clip(x, -1.0, 1.0)\n        f = func(x_clean)\n        self.evals += 1\n        if f < self.best_f:\n            self.best_f = f\n            self.best_x = x_clean.copy()\n        return f\n\n    def __call__(self, func, grad_func=None):\n        if self.evals >= self.budget: return self.best_f, self.best_x\n        \n        # LHS Initialization\n        pop = _lhs(n_samples=self.pop_size, n_dim=24)\n        pop = np.clip(pop * 2.0 - 1.0, -1.0, 1.0)\n        \n        fitness = np.full(self.pop_size, float('inf'))\n        for i in range(self.pop_size):\n            fitness[i] = self._evaluate(pop[i], func)\n            if self.evals >= self.budget: return self.best_f, self.best_x\n            \n        # Parameters\n        mu_F, mu_CR = 0.0, 0.5\n        max_gen = min(self.pop_size * 2, max(1, self.budget // self.pop_size))\n        \n        for gen in range(max_gen):\n            # Early termination to preserve budget for final refinement\n            if self.evals >= self.budget * 0.88: break\n            \n            best_idx = np.argmin(fitness)\n            x_best = pop[best_idx]\n            gen_sF, gen_sCR = [], []\n            \n            # Compute gradient bias ONCE per generation to drastically reduce expensive calls\n            grad_bias = np.zeros(24)\n            if grad_func:\n                try:\n                    g = grad_func(x_best)\n                    if g is not None and len(g) == 18:\n                        grad_bias[:18] = np.nan_to_num(g, nan=0.0, posinf=0.0, neginf=0.0)\n                        norm_g = np.linalg.norm(grad_bias)\n                        if norm_g > 1e-8:\n                            grad_bias[:18] *= 0.04  # Conservative step to maintain exploration\n                except Exception:\n                    pass\n                    \n            for i in range(self.pop_size):\n                if self.evals >= self.budget: break\n                \n                # jADE adaptive parameters\n                F_i = max(0.1, min(1.0, self.rng.normal(mu_F, 0.1)))\n                CR_i = max(0.0, min(1.0, self.rng.normal(mu_CR, 0.1)))\n                \n                idxs = self.rng.choice(self.pop_size, 3, replace=False)\n                while np.any(idxs == i): idxs = self.rng.choice(self.pop_size, 3, replace=False)\n                r1, r2, r3 = idxs\n                \n                # Mutation: DE/best/1 + global gradient bias\n                v = x_best + F_i * (pop[r1] - pop[r2]) + grad_bias\n                \n                # Cliff-aware Cauchy jump for high-dimensional escaping\n                cauchy_scale = 0.02 + 0.06 * np.exp(np.clip(-fitness[best_idx] + 4.0, 0.0, 3.0))\n                v += cauchy_scale * self.rng.standard_cauchy(self.dim)\n                v = np.clip(v, -1.0, 1.0)\n                \n                # Crossover\n                cr_mask = self.rng.random(self.dim) < CR_i\n                trial = np.where(cr_mask, v, pop[i])\n                \n                f_trial = self._evaluate(trial, func)\n                \n                if f_trial < fitness[i]:\n                    pop[i] = trial\n                    fitness[i] = f_trial\n                    gen_sF.append(F_i)\n                    gen_sCR.append(CR_i)\n                else:\n                    gen_sF.append(0.0)\n                    gen_sCR.append(0.0)\n            \n            # Update jADE means\n            if gen_sF:\n                mu_F = 0.1 * mu_F + 0.9 * np.mean(gen_sF)\n                mu_CR = max(0.0, min(1.0, 0.1 * mu_CR + 0.9 * np.mean(gen_sCR)))\n                \n        # Strictly budget-constrained local search\n        if grad_func and self.evals < self.budget * 0.75:\n            n_starts = min(2, self.pop_size)\n            best_indices = np.argsort(fitness)[:n_starts]\n            \n            for idx in best_indices:\n                if self.evals >= self.budget * 0.92: break\n                x0 = pop[idx]\n                fixed_cats = x0[18:]\n                cur_cont = x0[:18]\n                \n                def cost_wrap(x_c): \n                    return self._evaluate(np.concatenate([x_c, fixed_cats]), func)\n                    \n                def grad_wrap(x_c):\n                    full = np.concatenate([x_c, fixed_cats])\n                    try:\n                        g = grad_func(full)\n                        return np.nan_to_num(g, nan=0.0, posinf=0.0, neginf=0.0) if g is not None else np.zeros(18)\n                    except Exception:\n                        return np.zeros(18)\n                \n                # Hard limits on local search iterations to prevent timeout\n                res = minimize(cost_wrap, cur_cont, jac=grad_wrap, method='L-BFGS-B', \n                               bounds=[(-1.0, 1.0)]*18, options={'maxiter': 4, 'ftol': 1e-5})\n                \n                if np.isfinite(res.fun) and res.fun < self.best_f:\n                    self.best_f = res.fun\n                    self.best_x = np.concatenate([res.x, fixed_cats])\n                    \n        return self.best_f, self.best_x",
    "generation": 5,
    "name": "Optimizer"
}